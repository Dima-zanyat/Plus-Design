# Project Плюс Дизайн

## 2026-08-15 - Описание документации проекта
Описание документации приложения. Для ИИ агентов создан отдельный файл AGENTS.md в котором описаны правила работы с фронтендом и бэкендом, а также общие правила работы с кодом. В этом файле содержатся инструкции по структуре репозитория, правила для фронтенда и бэкенда, команды для запуска и тестирования, а также подсказки для агентов.
Создана структура документации REDME.md , AGENTS.md docs/

### Контекст
    Проекту нужна документация, которая будет полезна для разработчиков и агентов ИИ, работающих с проектом. В документации описаны правила работы с фронтендом и бэкендом, структура репозитория, команды для запуска и тестирования, а также подсказки для агентов.
### Решения
    README.md, AGENTS.md, docs/

### Результат
    Документация проекта PlusDesign, которая содержит информацию о проекте, структуре репозитория, правилах работы с фронтендом и бэкендом, командах для запуска и тестирования, а также подсказки для агентов ИИ.
## 2026-08-16 — Первая итерация бэкенда

### Контекст
Нужен минимальный, но расширяемый бэкенд: асинхронное FastAPI-приложение,
асинхронная работа с БД, витрина портфолио с пагинацией и форма-заявка
на проект. Фронтенд отложен на следующую итерацию.

### Решения

**PostgreSQL + asyncpg вместо SQLite.**
Продакшн-путь берём сразу: нормальные типы (`ENUM`, `TIMESTAMPTZ`),
конкурентная запись, полноценные миграции. Локально поднимается через
`docker-compose.yml`. Цена — нужен запущенный контейнер для разработки.

**SQLAlchemy 2.0 async + Alembic.**
`create_async_engine` + `async_sessionmaker`, типизированный стиль
`Mapped[...]`/`mapped_column`. Alembic настроен на асинхронный движок,
URL подставляется из `app.config`, а не из `alembic.ini`.

**Пагинация offset/limit, серверная.**
Витрина портфолио — это сетка с номерами страниц, где нужен переход на
произвольную страницу и общее число страниц. Курсорная пагинация это не
даёт. Срез делает БД (`OFFSET/LIMIT` + отдельный `COUNT`), наружу уходит
только текущая страница. Сортировка `created_at DESC, id DESC` —
вторичный ключ нужен для детерминированного порядка при одинаковых датах,
иначе одна и та же работа может попасть на две страницы. Под неё заведён
индекс `ix_portfolio_items_created_at_id`.

Ограничение подхода: на больших смещениях `OFFSET` деградирует, а вставка
новых работ во время листания сдвигает элементы. Для портфолио в сотни
записей это не проблема. Схема `Page[T]` универсальна, поэтому курсорный
вариант можно добавить рядом, не ломая существующий контракт.

**Модель портфолио — минимальная.**
Только `title`, `slug`, `description`, `cover_image`, `created_at`.
Категории, галерея изображений, `is_published` и `sort_order` сознательно
отложены до появления реального контента.

**Форма-заявка: имя и телефон обязательны, email опционален.**
Телефон нормализуется на уровне Pydantic-схемы к виду `+<цифры>`,
российский формат `8XXXXXXXXXX` приводится к `+7XXXXXXXXXX`. Благодаря
этому проверка дубликатов работает по каноническому номеру. Взяли регулярку,
а не библиотеку `phonenumbers`: одна зависимость меньше, а строгая проверка
операторских кодов нам пока не нужна.

Антидубль: повторная заявка с того же номера в течение 5 минут отклоняется
с `409`. Это защита от двойного клика по кнопке, а не rate limiting —
полноценное ограничение по IP появится вместе с реверс-прокси.

**Слоистая архитектура с зависимостями внутрь.**
`endpoints → services → repositories → models`. Сервисы не импортируют
FastAPI, а зависят от `typing.Protocol`, описывающего нужный им репозиторий.
Протокол объявлен в самом сервисном модуле, чтобы домен не смотрел наружу.
Так сервис тестируется на заглушке без БД.

**Транзакции — на уровне зависимости `get_db`.**
Один HTTP-запрос — одна транзакция: коммит при успехе, откат при любом
исключении. Репозитории делают только `flush`, чтобы получить `id`.
Альтернатива — коммит внутри сервиса — размывала бы границы транзакции
между несколькими сервисами в одном запросе.

**Доменные исключения вместо `HTTPException` в сервисах.**
`NotFoundError`, `ConflictError`, `ValidationError` переводятся в 404/409/422
обработчиками в `app/main.py`. Сервисный слой остаётся независимым от HTTP.

**Тесты по умолчанию на SQLite в памяти.**
`pytest` запускается без внешней инфраструктуры; для проверки на реальном
движке достаточно задать `TEST_DATABASE_URL` с asyncpg. Риск расхождения
диалектов есть, поэтому корректность SQL миграции проверяется отдельно
через `alembic upgrade head --sql`.

### Результат
18 тестов проходят, `ruff check` чистый, миграция `0001_init` генерирует
корректный PostgreSQL-SQL. Доступны эндпоинты: `GET /api/v1/health`,
`GET /api/v1/health/db`, `GET /api/v1/portfolio`, `GET /api/v1/portfolio/{slug}`,
`POST /api/v1/portfolio`, `POST /api/v1/leads`. Контракт описан в `docs/api.md`.

### Открытые вопросы
- `POST /api/v1/portfolio` пока без авторизации — закрыть вместе с админкой.
- Загрузка изображений не реализована: `cover_image` хранит строку-путь.
- Уведомление о новой заявке (почта или мессенджер) не сделано.

## 2026-08-19 — Контекст продукта и UI

### Контекст
Ранние формулировки описывали проект как витрину графики, веба и брендинга.
Фактически это сайт-визитка дизайнера интерьера. Japandi по ошибке читался
как направление интерьеров, хотя это только визуальный язык интерфейса.

### Решения
- Бренд: **Плюс Дизайн**. Автор: **Анастасия Плюснина** — дизайнер-проектировщик
  и визуализатор, 4 года практики, интерьеры квартир и домов, авторский надзор.
- Задача сайта: портфолио + приём заявок.
- Japandi остаётся в `docs/fronted.md` как стиль UI и не используется в
  пользовательских слоганах и описании услуг.
- Документация и публичные тексты фронтенда приведены к этому контексту.

### Результат
Агенты и разработчики читают один смысл продукта в README, AGENTS.md и docs/.

## 2026-08-30 — Админ-API, JWT, прод-Docker

### Контекст
Публичная витрина и заявки уже работали, но запись в портфолио была открытой,
`jwt==1.4.0` не совпадал с `jwt.encode`/`decode` (нужен PyJWT), compose был
dev-сценарием с паролями в файле и healthcheck на несуществующий `/health`.

### Решения
- Запись портфолио только под Bearer JWT: `POST/PATCH/DELETE /api/v1/admin/portfolio…`.
  Публичные `GET /portfolio` и `GET /portfolio/{slug}` не ломаем: админский
  префикс `/admin`, чтобы `{slug}` не перехватывал `admin`.
- Неверный логин и пароль — один `UnauthorizedError` → HTTP 401.
- В `production` отключены `/docs` и `/openapi.json`. Медиа отдаются как
  `StaticFiles`; в работах хранятся URL, без загрузки бинарников.
- JWT: зависимость **PyJWT**, не пакет `jwt`.
- Прод: корневой `docker-compose.yml` (`db` + `backend` + `frontend` nginx :80),
  секреты из `.env`. Локалка: `docker-compose.dev.yml` (Vite + прокси на backend).
  Healthcheck: `GET /api/v1/health` и `/api/v1/health/db`.

### Результат
Админка на сайте (`/admin/login`, таблица и форма работ), закрытый write API,
документация и примеры env обновлены.

## 2026-08-30 — Загрузка изображений вместо ручного ввода URL

### Контекст
Форма работы в админке принимала обложку и галерею только как текстовые
поля URL — админу нужно было заранее где-то разместить картинку и вручную
скопировать ссылку. Ни на бэкенде, ни на фронтенде эндпоинта загрузки файла
не было, хотя `MEDIA_ROOT`/`MEDIA_URL_PREFIX` в настройках уже
существовали и статика `/media` уже монтировалась в `main.py`.

### Решения
- Новый `POST /api/v1/admin/media/upload` (`app/api/v1/endpoints/media.py`):
  multipart-загрузка одного файла, за той же зависимостью `get_current_admin`,
  что и остальная админка. Проверяет content-type (только
  JPEG/PNG/WEBP/GIF) и размер (≤8 МБ), сохраняет под случайным именем в
  `MEDIA_ROOT`, возвращает `{ "url": "/media/..." }`.
- Добавлен `python-multipart` в `requirements.txt` и `pyproject.toml` —
  без него FastAPI не может парсить `UploadFile`.
- На фронтенде `AdminItemFormPage.tsx` переписан: вместо текстовых полей —
  `<input type="file">` для обложки (одна картинка, с превью и кнопкой
  «Убрать») и для галереи (множественная загрузка, список миниатюр,
  удаление по одной). Загруженный `url` кладётся в те же поля
  `cover_image`/`images`, что и раньше — формат данных, который ждёт API,
  не менялся.
- Попутно найден и исправлен баг в `api/client.ts`: заголовок
  `Content-Type: application/json` подставлялся всегда, включая запросы с
  `FormData` — это ломает multipart-boundary, который браузер должен
  выставить сам. Теперь заголовок ставится только для JSON-тел.

### Результат
Админ выбирает файл с диска — обложка или кадр галереи загружается,
превью показывается сразу в форме, дальше сохранение работы происходит
как раньше. Ручной ввод URL по-прежнему возможен (поле не заблокировано
для уже сохранённых значений, просто теперь есть и загрузка) — на бэкенде
это осталось той же строкой `cover_image`/`images[].url`.

Проверено: 31/31 backend-тест зелёный (добавлен `tests/test_media.py`,
4 теста), `ruff check` чист на изменённых файлах, `npm run build`
(tsc + vite) и `npm run lint` (oxlint) — 0 ошибок на фронтенде.

### Открытые вопросы
- Множественная загрузка есть только для галереи; за один запрос всё
  равно уходит по одному файлу (цикл на клиенте), не batch-эндпоинт.
- Нет удаления файла с диска при отвязывании изображения от работы —
  осиротевшие файлы в `MEDIA_ROOT` не подчищаются.
- Нет ограничения на количество файлов в галерее за раз.

## 2026-08-30 — Просмотр заявок в админке

### Контекст
`LeadService` уже содержал `list_leads()` и `update_status()` — методы
были написаны, но никогда не подключались к HTTP-слою. В README (раздел
«Что дальше») это было явно отмечено как пропуск («Админка заявок»).
Без SMTP/Telegram-уведомлений (оба опциональны) заявки с формы на сайте
уходили в БД без единого способа их увидеть — критично перед реальным
запуском на сервер.

### Решения
- Новый `admin_leads.py`: `GET /api/v1/admin/leads` (список, пагинация,
  новые сверху — уже так сортировал репозиторий) и
  `PATCH /api/v1/admin/leads/{id}` (смена статуса: `new` / `in_progress` /
  `done` / `rejected`). Оба — за `get_current_admin`, тем же способом,
  что и остальная админка.
- Фронтенд: `/admin/leads` — таблица с датой, именем, телефоном (кликабелен
  как `tel:`), email (`mailto:`), сообщением и выпадающим статусом.
  Ссылка добавлена в `AdminHeader`.
- Заодно убрал из репозитория случайно закоммиченный мусор:
  `backend/_probe.txt` (отладочный файл с текстом «test») и
  `backend/plusdesign_backend.egg-info/` (артефакт сборки setuptools,
  такие файлы не должны попадать в git). Добавил `*.egg-info/` в
  `.gitignore`.

### Результат
36/36 backend-тестов (добавлен `test_admin_leads.py`, 5 тестов), ruff чист
на изменённых файлах. Фронтенд: `npm run build` (tsc + vite) и
`npm run lint` (oxlint) — 0 ошибок. README обновлён: статус-таблица,
«Что дальше», дерево репозитория (структура отставала от кода — не
хватало `media.py`, `admin_leads.py`, `schemas/auth.py`, `schemas/media.py`,
тестовых файлов; упоминался несуществующий `backend/docker-compose.yml`).

### Открытые вопросы
- Нет пагинации на странице `/admin/leads` во фронтенде — грузится сразу
  до 100 заявок; если их станет больше, нужен постраничный вывод.
- Нет фильтра по статусу в UI (только сортировка «новые сверху»).
- CI по-прежнему не настроен.
